-e ../../packages/core

fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
email-validator>=2.1

# Phase 1: file parsing/validation used by tests and the upload endpoint's
# defense-in-depth checks (real parsing happens in apps/worker -- see
# CLAUDE.md Section 7.11).
pdfplumber>=0.11
python-docx>=1.1
openpyxl>=3.1

# Phase 1: enqueue-only Celery client (app/celery_client.py) -- the API
# never defines or runs task bodies, it only sends tasks by name onto the
# same broker apps/worker listens on.
celery[redis]>=5.4

pytest>=8.2
httpx>=0.27
pytest-cov>=5.0
ruff>=0.5
mypy>=1.10
