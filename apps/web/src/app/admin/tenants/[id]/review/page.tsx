/**
 * Onboarding Step 8 (Section 7.15.2): the founder opens a tenant's orders in
 * the normal review screen, acting as DocFlow support (Section 7.15.1). The
 * very same page a tenant user sees -- only its API prefix and links change
 * (src/lib/reviewScope.ts; D-111).
 */
export { default } from "@/app/review/page";
